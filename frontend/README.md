# Cinderella Cakes — Frontend

This is the React (Vite) frontend. See the root `README.md` (one folder up) for the
full setup guide covering both this and the PHP backend.

Quick start:
```
npm install
npm run dev
```
